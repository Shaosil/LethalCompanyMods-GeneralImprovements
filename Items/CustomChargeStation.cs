using Unity.Netcode;

namespace GeneralImprovements.Items
{
    public class CustomChargeStation : NetworkBehaviour
    {
        // Mostly used so we have something unique to search for after converting the vanilla item charger to a network prefab
    }
}